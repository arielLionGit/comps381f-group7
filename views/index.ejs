<%- include('partials/header') %>

<div class="container">
  <div class="post-list">
    <h1 style="margin-bottom: 2rem;">最新文章</h1>
    
    <% if (posts.length === 0) { %>
      <div class="card">
        <div class="card-body text-center">
          <p>目前還沒有任何文章</p>
          <% if (user || isAdmin) { %>
            <a href="/create" class="btn btn-primary mt-2">發表第一篇文章</a>
          <% } %>

        </div>
      </div>
    <% } else { %>
      <% posts.forEach(post => { %>
        <div class="post-item">
          <h2 class="post-title">
            <a href="/post/<%= post._id %>"><%= post.title %></a>
          </h2>
          <div class="post-meta">
            <span>👤 <%= post.authorName %></span>
            <span>📅 <%= new Date(post.createdAt).toLocaleDateString('zh-TW') %></span>
            <span>👁️ <%= post.viewCount %> 次瀏覽</span>
          </div>
          <div class="post-excerpt">
            <%= post.content.substring(0, 200) %><%= post.content.length > 200 ? '...' : '' %>
          </div>
          <% if (post.tags && post.tags.length > 0) { %>
            <div class="post-tags">
              <% post.tags.forEach(tag => { %>
                <a href="/tag/<%= tag %>" class="tag">#<%= tag %></a>
              <% }) %>
            </div>
          <% } %>
        </div>
      <% }) %>
    <% } %>

    <!-- 分頁 -->
    <% if (totalPages > 1) { %>
      <div class="pagination">
        <% if (currentPage > 1) { %>
          <a href="/?page=<%= currentPage - 1 %>">上一頁</a>
        <% } %>
        
        <% for (let i = 1; i <= totalPages; i++) { %>
          <% if (i === currentPage) { %>
            <span class="active"><%= i %></span>
          <% } else { %>
            <a href="/?page=<%= i %>"><%= i %></a>
          <% } %>
        <% } %>
        
        <% if (currentPage < totalPages) { %>
          <a href="/?page=<%= currentPage + 1 %>">下一頁</a>
        <% } %>
      </div>
    <% } %>
  </div>
</div>

<%- include('partials/footer') %>
