<%- include('partials/header') %>

<div class="container">
  <div style="max-width: 900px; margin: 3rem auto;">
    <div class="card">
      <div class="card-header">
        <h2>編輯文章</h2>
      </div>
      <div class="card-body">
        <% if (errors && errors.length > 0) { %>
          <div class="form-error">
            <ul>
              <% errors.forEach(error => { %>
                <li><%= error.msg %></li>
              <% }) %>
            </ul>
          </div>
        <% } %>

        <form action="/post/<%= post._id %>/edit" method="POST" enctype="multipart/form-data">
          <div class="form-group">
            <label for="title" class="form-label">標題 *</label>
            <input type="text" id="title" name="title" class="form-control" value="<%= post.title %>" required maxlength="200">
          </div>

          <div class="form-group">
            <label for="content" class="form-label">內容 *</label>
            <textarea id="content" name="content" class="form-control" required><%= post.content %></textarea>
          </div>

          <% if (post.images && post.images.length > 0) { %>
            <div class="form-group">
              <label class="form-label">現有圖片</label>
              <div class="post-images">
                <% post.images.forEach((image, index) => { %>
                  <div style="position: relative;">
                    <img src="<%= image.data || image.path %>" alt="<%= image.filename || '文章圖片' %>" class="post-image">
                    <label style="position: absolute; top: 10px; right: 10px; background: white; padding: 5px; border-radius: 5px;">
                      <input type="checkbox" name="removeImages" value="<%= index %>">
                      刪除
                    </label>
                  </div>
                <% }) %>
              </div>
            </div>
          <% } %>

          <div class="form-group">
            <label for="images" class="form-label">新增圖片（最多 5 張）</label>
            <input type="file" id="images" name="images" class="form-control" multiple accept="image/*">
            <small style="color: var(--text-light);">支援格式：JPEG, JPG, PNG, GIF, WebP，每張最大 5MB</small>
          </div>

          <div class="form-group">
            <label for="tags" class="form-label">標籤</label>
            <input type="text" id="tags" name="tags" class="form-control" value="<%= post.tags ? post.tags.join(', ') : '' %>" placeholder="例如：技術,生活,旅遊（用逗號分隔）">
          </div>

          <div style="display: flex; gap: 1rem;">
            <button type="submit" class="btn btn-primary">更新文章</button>
            <a href="/post/<%= post._id %>" class="btn btn-outline">取消</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>

<%- include('partials/footer') %>
