<%- include('../partials/header') %>

<div class="container admin-dashboard">
  <h1 style="margin-bottom: 2rem;">Post Management</h1>

  <div class="card">
    <div class="card-body">
      <table class="table">
        <thead>
          <tr>
            <th>Title</th>
            <th>Author</th>
            <th>Published At</th>
            <th>Views</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% posts.forEach(post => { %>
            <tr>
              <td><a href="/post/<%= post._id %>"><%= post.title %></a></td>
              <td><%= post.authorName %></td>
              <td><%= new Date(post.createdAt).toLocaleString('en-US') %></td>
              <td><%= post.viewCount %></td>
              <td>
                <a href="/post/<%= post._id %>/edit" class="btn btn-sm btn-secondary">Edit</a>
                <form action="/post/<%= post._id %>/delete" method="POST" style="display: inline;" onsubmit="return confirm('Delete this post?');">
                  <button type="submit" class="btn btn-sm btn-danger">Delete</button>
                </form>
              </td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </div>

  <!-- Pagination -->
  <% if (totalPages > 1) { %>
    <div class="pagination">
      <% if (currentPage > 1) { %>
        <a href="/admin/posts?page=<%= currentPage - 1 %>">Previous</a>
      <% } %>
      
      <% for (let i = 1; i <= totalPages; i++) { %>
        <% if (i === currentPage) { %>
          <span class="active"><%= i %></span>
        <% } else { %>
          <a href="/admin/posts?page=<%= i %>"><%= i %></a>
        <% } %>
      <% } %>
      
      <% if (currentPage < totalPages) { %>
        <a href="/admin/posts?page=<%= currentPage + 1 %>">Next</a>
      <% } %>
    </div>
  <% } %>
</div>

<%- include('../partials/footer') %>
